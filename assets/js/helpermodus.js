(function($) {
    'use strict';

    $(document).ready(function() {
        console.log('helpermodus script loaded');
        // Weitere Helpermodus-spezifische Logik kann hier ergänzt werden
    });
})(jQuery);
